import { createFileRoute } from "@tanstack/react-router";import { useState } from "react";import { Button } from "@/components/ui/button";import { Dialog,DialogContent,DialogDescription,DialogHeader,DialogTitle,DialogTrigger } from "@/components/ui/dialog";import { detailLinkClass,eventCategoryClass,PageIntro,PhotoPlaceholder,SketchArrow } from "@/components/site";import { events } from "@/lib/content";
export const Route=createFileRoute("/events")({head:()=>({meta:[{title:"Etkinlikler — YZT"},{name:"description",content:"YZT yaklaşan ve geçmiş etkinlikleri."},{property:"og:title",content:"Etkinlikler — YZT"},{property:"og:description",content:"Seminer, atölye, söyleşi ve teknik geziler."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Events});
 function Events(){const [filter,setFilter]=useState("Tümü");const shown=filter==="Tümü"?events:events.filter(e=>e.status===filter);return <><PageIntro eyebrow="Etkinlikler · 01" title="Dinle, dene, birlikte üret."><p>Seminerlerden teknik gezilere kadar her buluşma yeni bir soru ve yeni bir bağlantı için.</p></PageIntro><section className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="mb-10 flex flex-wrap items-center gap-2 border-b border-brand-mid pb-5" aria-label="Etkinlik filtresi"><span className="section-marker mr-auto">02 / Takvim</span>{["Tümü","Yaklaşan","Geçmiş"].map(x=><Button key={x} variant={filter===x?"default":"outline"} onClick={()=>setFilter(x)}>{x}</Button>)}</div><div className="grid gap-x-7 gap-y-16 md:grid-cols-2 lg:grid-cols-3">{shown.map((e,i)=><article key={e.title} className={`event-preview ${eventCategoryClass(e.tag)}`}><PhotoPlaceholder label={`${e.title} etkinliği`} index={i}/><div className="mt-5 flex items-center gap-3 text-xs font-bold uppercase"><span className="text-primary">{e.tag}</span><span className="text-brand-dark">{e.date}</span></div><h2 className="mt-3 font-display text-3xl">{e.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{e.desc}</p><Dialog><DialogTrigger asChild><Button variant="link" className={`mt-4 ${detailLinkClass}`}>Detay <SketchArrow className="transition-transform group-hover:translate-x-1"/></Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle className="font-display text-3xl">{e.title}</DialogTitle><DialogDescription className="pt-2 leading-6">{e.date} · {e.tag}</DialogDescription></DialogHeader><PhotoPlaceholder label={`${e.title} etkinliği`} index={i}/><p className="leading-7 text-muted-foreground">{e.desc} Program ve katılım bilgileri etkinlik tarihinden önce Instagram hesabımızda paylaşılacaktır.</p></DialogContent></Dialog></article>)}</div></section></>}
